name: Telegram Issue Reply Notify

on:
  workflow_call:
    inputs:
      date_format:
        description: "Date format"
        required: false
        type: string
      calendar_type:
        description: "Calendar type"
        required: false
        type: string
        default: "jalali"
    secrets:
      bot_token:
        description: "Telegram bot token"
        required: true
      chat_id:
        description: "Target Telegram chat ID"
        required: true

jobs:
  notify-reply-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        if: inputs.date_format != '' && inputs.date_format != null
        uses: actions/checkout@v4

      - name: Setup PHP
        if: inputs.date_format != '' && inputs.date_format != null
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: intl

      - name: Install Composer and MultiCalendar package
        if: inputs.date_format != '' && inputs.date_format != null
        run: |
          echo "=== Checking Composer ==="
          if command -v composer &> /dev/null; then
            echo "✅ Composer is installed."
            echo "Composer version: $(composer -V | head -1)"
          else
            echo "❌ Composer is not installed. Attempting to install..."
            curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
            if command -v composer &> /dev/null; then
              echo "✅ Composer installed successfully."
            else
              echo "❌ Failed to install Composer. Date conversion may fail."
              exit 1
            fi
          fi

          echo ""
          echo "=== Installing MultiCalendar package (latest version) ==="
          composer require jobmetric/multi-calendar --prefer-dist --no-interaction
          
          if [ $? -eq 0 ]; then
            echo "✅ MultiCalendar package installed successfully"
            if [ -f "vendor/autoload.php" ]; then
              echo "✅ Autoload file found: vendor/autoload.php"
            else
              echo "⚠️  Autoload file not found"
            fi
          fi

      - name: Create Date Converter Script
        if: inputs.date_format != '' && inputs.date_format != null
        run: |
          echo "=== Creating convert-date.sh script ==="
          
          cat > convert-date.sh << 'SCRIPT_EOF'
          #!/bin/bash
          DATE="$1"
          TO_CAL="${2:-jalali}"
          
          if [ -z "$DATE" ]; then
            echo "Error: Date is required" >&2
            exit 1
          fi
          
          if [ ! -f "vendor/autoload.php" ]; then
            echo "Error: vendor/autoload.php not found" >&2
            exit 1
          fi
          
          MC_DATE="$DATE" MC_TO="$TO_CAL" php -r '
          require_once "vendor/autoload.php";
          use JobMetric\MultiCalendar\Factory\CalendarConverterFactory;
          
          $date = getenv("MC_DATE");
          $toCal = getenv("MC_TO");
          
          try {
              $dateTime = new DateTime($date);
              $gy = (int)$dateTime->format("Y");
              $gm = (int)$dateTime->format("m");
              $gd = (int)$dateTime->format("d");
              
              $converter = CalendarConverterFactory::make($toCal);
              $result = $converter->fromGregorian($gy, $gm, $gd, "-");
              
              if (is_string($result)) {
                  echo $result;
              } else {
                  echo sprintf("%04d-%02d-%02d", $result[0], $result[1], $result[2]);
              }
          } catch (Throwable $e) {
              fwrite(STDERR, "Error: " . $e->getMessage());
              exit(1);
          }
          '
          SCRIPT_EOF
          
          chmod +x convert-date.sh
          echo "✅ Script created: convert-date.sh"
          echo "✅ Script is executable"
          echo "✅ Script size: $(wc -c < convert-date.sh) bytes"

      - name: Send Telegram notification for issue comment
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.bot_token }}
          TELEGRAM_CHAT_ID: ${{ secrets.chat_id }}
          DATE_FORMAT: ${{ inputs.date_format }}
          CALENDAR_TYPE: ${{ inputs.calendar_type }}
        shell: bash
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            exit 0
          fi

          COMMENT_BODY="${{ github.event.comment.body }}"
          ISSUE_TITLE="${{ github.event.issue.title }}"
          ISSUE_NUMBER="${{ github.event.issue.number }}"
          ISSUE_URL="${{ github.event.issue.html_url }}"
          COMMENT_URL="${{ github.event.comment.html_url }}"
          REPO="${{ github.repository }}"
          AUTHOR="${{ github.event.comment.user.login }}"
          CREATED_AT="${{ github.event.comment.created_at }}"
          
          CREATED_DATE=$(date -d "$CREATED_AT" "+%Y-%m-%d" 2>/dev/null || echo "$CREATED_AT")
          
          if [ -n "$DATE_FORMAT" ]; then
            echo "=== Converting date ==="
            echo "📅 Original date: $CREATED_AT"
            echo "📋 Date format: $DATE_FORMAT"
            
            if [ ! -f "./convert-date.sh" ]; then
              echo "❌ convert-date.sh not found in current directory"
              echo "🔍 Current directory: $(pwd)"
              echo "🔍 Files in current directory:"
              ls -la | head -10
              echo "🔍 Searching for script..."
              find . -name "convert-date.sh" -type f 2>/dev/null | head -5
              echo "⚠️  Date conversion skipped, using Gregorian date"
            elif [ ! -f "vendor/autoload.php" ]; then
              echo "❌ vendor/autoload.php not found"
              echo "⚠️  Date conversion skipped, using Gregorian date"
            else
              TARGET_CAL="${DATE_FORMAT}"
              if [ "$DATE_FORMAT" == "both" ]; then
                 TARGET_CAL="${CALENDAR_TYPE:-jalali}"
              fi
              
              echo "🔄 Target calendar: $TARGET_CAL"
              echo "🔧 Converting date..."
              echo "📁 Script path: $(pwd)/convert-date.sh"
              echo "📁 Script exists: $([ -f "./convert-date.sh" ] && echo "yes" || echo "no")"
              echo "📁 Script executable: $([ -x "./convert-date.sh" ] && echo "yes" || echo "no")"
              
              chmod +x ./convert-date.sh 2>/dev/null || true
              echo "🔧 Running: ./convert-date.sh \"$CREATED_AT\" --to \"$TARGET_CAL\" --output-format \"Y-m-d\""
              
              CONVERTED=$(./convert-date.sh "$CREATED_AT" --to "$TARGET_CAL" --output-format "Y-m-d" 2>&1)
              CONVERT_EXIT_CODE=$?
              
              echo "📊 Exit code: $CONVERT_EXIT_CODE"
              echo "📊 Output: $CONVERTED"
              
              if [ $CONVERT_EXIT_CODE -eq 0 ] && [ -n "$CONVERTED" ] && [[ ! "$CONVERTED" =~ ^Error ]]; then
                echo "✅ Date converted: $CONVERTED"
                if [ "$DATE_FORMAT" == "both" ]; then
                  CREATED_DATE="${CREATED_DATE} / ${CONVERTED} (${TARGET_CAL})"
                else
                  CREATED_DATE="${CONVERTED} (${TARGET_CAL})"
                fi
              else
                echo "⚠️  Date conversion failed, using Gregorian date"
                if [ $CONVERT_EXIT_CODE -eq 127 ]; then
                  echo "❌ Script not found or not executable (exit code 127)"
                fi
              fi
            fi
          fi
          
          echo "📅 Final date: $CREATED_DATE"

          escape_html() { echo "$1" | sed 's/&/\&amp;/g; s/</\&lt;/g; s/>/\&gt;/g; s/"/\&quot;/g'; }
          ISSUE_TITLE_ESC=$(escape_html "$ISSUE_TITLE")
          COMMENT_BODY_ESC=$(escape_html "$COMMENT_BODY" | head -c 1000)

          MESSAGE="New comment on issue <b><code>${REPO}</code> #${ISSUE_NUMBER}</b>"$'\n\n'
          MESSAGE="${MESSAGE}Title issue: <b>${ISSUE_TITLE_ESC}</b>"$'\n\n'
          
          if [ -n "$COMMENT_BODY_ESC" ] && [ "$COMMENT_BODY_ESC" != "null" ] && [ "$COMMENT_BODY_ESC" != "" ]; then
            MESSAGE="${MESSAGE}Comment issue: ${COMMENT_BODY_ESC}"$'\n\n'
          fi

          MESSAGE="${MESSAGE}Commented by: <b><code>${AUTHOR}</code></b>"$'\n'
          MESSAGE="${MESSAGE}Time: <b>${CREATED_DATE}</b>"$'\n\n'
          MESSAGE="${MESSAGE}<a href=\"${ISSUE_URL}\">View Issue on GitHub</a>"$'\n'
          MESSAGE="${MESSAGE}<a href=\"${COMMENT_URL}\">View Comment on GitHub</a>"

          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            --data-urlencode "text=$MESSAGE" \
            -d parse_mode="HTML" \
            -d disable_web_page_preview=true